version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - setup_remote_docker
      - checkout
      - run: echo "hello jeremy"
      - run: docker build -t circle-test .
          
      